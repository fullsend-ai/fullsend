{
  "name": "fullsend-docs",
  "version": "0.22.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "git -C .. submodule update --init",
    "prebuild": "git -C .. submodule update --init",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "mermaid": "~11.16.0",
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "engines": {
    "node": ">=18.0"
  }
}
