{
  "name": "fullsend-docs",
  "version": "0.22.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "git -C .. submodule update --init && ./scripts/fetch-agent-docs.sh",
    "prebuild": "git -C .. submodule update --init && ./scripts/fetch-agent-docs.sh --force",
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "dependencies": {
    "@vueuse/core": "^12.4.0",
    "@vueuse/integrations": "^12.4.0",
    "mark.js": "8.11.1",
    "mermaid": "~11.16.0",
    "minisearch": "^7.1.1",
    "vitepress": "1.6.4",
    "vue": "^3.5.13"
  },
  "engines": {
    "node": ">=18.0"
  }
}
