{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fullsend.sh/schemas/entity-context/v1/thread-state.schema.json",
  "title": "Fullsend entity-context thread state v1",
  "type": "object",
  "required": ["threads"],
  "properties": {
    "threads": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "thread_key", "created_at", "order_path", "comment_ids", "resolved", "outdated"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "thread_key": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "created_at": { "type": "string", "format": "date-time" },
          "order_path": { "type": "string", "pattern": "^threads/[0-9a-f]{64}[.]order$" },
          "comment_ids": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "uniqueItems": true
          },
          "resolved": { "type": "boolean" },
          "outdated": { "type": "boolean" },
          "minimized": { "type": "boolean" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
